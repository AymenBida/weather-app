{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "> brief description",
  "main": "webpack.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/js/index.ts --bundle --outfile=dist/main.js",
    "watch": "esbuild src/js/index.ts --bundle --watch --outfile=dist/main.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.25.4"
  },
  "dependencies": {
  }
}
